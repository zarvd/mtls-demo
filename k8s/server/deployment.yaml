apiVersion: apps/v1
kind: Deployment
metadata:
  name: mtls-server
  namespace: mtls-server
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mtls-server
  template:
    metadata:
      labels:
        app: mtls-server
    spec:
      containers:
        - name: mtls-server
          image: ghcr.io/zarvd/mtls-demo/server:27f5e6efe4912b392e828f15c065c18c7259f65f
          command:
            - /usr/local/bin/mtls-server
            - --ca-bundle=/certs/ca/ca-bundle.pem
            - --certificate=/certs/server/tls.crt
            - --key=/certs/server/tls.key
            - --port=8443
          ports:
            - containerPort: 8443
          volumeMounts:
            - name: server-certs
              mountPath: /certs/server
              readOnly: true
            - name: ca-bundle
              mountPath: /certs/ca
              readOnly: true
          resources:
            requests:
              cpu: 100m
              memory: 100Mi
            limits:
              cpu: 1000m
              memory: 1000Mi
      volumes:
        - name: server-certs
          secret:
            secretName: server-secret
        - name: ca-bundle
          configMap:
            name: intra-cluster-mtls-ca-bundle
